// Test stub for the `server-only` package. In a real Next.js build, importing
// `server-only` from a client bundle throws; in tests we run server modules
// directly under Node, so this is a harmless no-op.
export {};
